{
  "name": "sprint-poker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:dev": "tsc -b && vite build --mode development",
    "build:prod": "tsc -b && vite build --mode production",
    "lint": "eslint .",
    "type-check": "tsc -b",
    "preview": "vite preview",
    "deploy:dev": "firebase use dev && npm run build:dev && firebase deploy",
    "deploy:prod": "firebase use production && npm run build:prod && firebase deploy",
    "deploy:rules:dev": "firebase use dev && firebase deploy --only firestore:rules",
    "deploy:rules:prod": "firebase use production && firebase deploy --only firestore:rules",
    "deploy:indexes:dev": "firebase use dev && firebase deploy --only firestore:indexes",
    "deploy:indexes:prod": "firebase use production && firebase deploy --only firestore:indexes"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "nanoid": "^6.0.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.5",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.6.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.1"
  }
}
